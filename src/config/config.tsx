// Useless Unit Config File 

// Matomo Monitoring
const use_matomo = false;
const matomo_url = "<MATOMO URL>";
const matomo_site_id = 0;



// exporting config
const config = {
    matomo: {
        use_matomo: use_matomo,
        matomo_url: matomo_url,
        matomo_site_id: matomo_site_id
    }
}

export default config;