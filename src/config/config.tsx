// Useless Unit Config File 

// Matomo Monitoring
const use_matomo = false;
const matomo_url = "";
const matomo_site_id = 1;



// exporting config
const config = {
    matomo: {
        use_matomo: use_matomo,
        matomo_url: matomo_url,
        matomo_site_id: matomo_site_id
    }
}

export default config;